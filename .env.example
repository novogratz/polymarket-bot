# Copy this file to .env and fill in your values. Never commit .env.
#
# This file contains only secrets, endpoints, paths, and machine-specific
# state. Strategy settings belong in `configs/profiles/*.toml`.
#
# `apply_profile_to_env(override=False)` does not overwrite variables already
# defined by `.env`; placing strategy keys here can silently shadow a profile.
# See docs/PROFILES.md for configuration guidance.

# ---- Authentication (required for live trading) ----
POLYMARKET_PRIVATE_KEY=0x_replace_me
POLYMARKET_FUNDER_ADDRESS=0x_replace_me
POLYMARKET_API_KEY=
POLYMARKET_API_SECRET=
POLYMARKET_API_PASSPHRASE=
POLYMARKET_SIGNATURE_TYPE=1

# ---- Endpoints ----
POLYMARKET_GAMMA_URL=https://gamma-api.polymarket.com
POLYMARKET_CLOB_URL=https://clob.polymarket.com
POLYMARKET_DATA_API_URL=https://data-api.polymarket.com

# ---- Paths ----
POLYMARKET_STATE_PATH=data/paper_state.json
POLYMARKET_TRADE_JOURNAL_PATH=data/trade_journal.jsonl
POLYMARKET_STRATEGY_OVERRIDES_PATH=data/strategy_overrides.json

# ---- Machine state ----
# Assumed live bankroll before the first CLOB sync. Use 0 to make no assumption.
# A profile may override this with [sizing].assumed_live_balance_usd.
POLYMARKET_ASSUME_LIVE_BALANCE_USD=0
# Synchronize live Polymarket positions into the local ledger.
POLYMARKET_SYNC_LIVE_POSITIONS=1

# --- Telegram alerts (push-only, best-effort) ---
# Disabled while TELEGRAM_BOT_TOKEN is empty. Create a bot with @BotFather,
# send it a message, and obtain the destination chat ID through Telegram's
# documented Bot API workflow.
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID_LIVE=
# TELEGRAM_CHAT_ID_DRY_RUN=
# TELEGRAM_ALERT_TRADES=1
# TELEGRAM_ALERT_ERRORS=1
# TELEGRAM_ALERT_THRESHOLDS=1
# TELEGRAM_ALERT_HEARTBEAT=1
# TELEGRAM_HEARTBEAT_MINUTES=30
# TELEGRAM_DRAWDOWN_PCT=10.0
# TELEGRAM_BIG_WIN_USD=10.0
# TELEGRAM_BIG_LOSS_USD=5.0
# TELEGRAM_EQUITY_FLOOR_USD=50.0
# TELEGRAM_DEDUPE_WINDOW_SEC=300
